# FakeCall App — 产品需求文档 (PRD)

**版本**: v1.0
**平台**: Android (Kotlin)
**更新日期**: 2026-04-04

---

## 1. 产品概述

FakeCall 是一款帮助用户从不想继续的谈话、场合或活动中优雅脱身的工具类 App。用户可以触发一个逼真的模拟来电，接听后进入通话界面，整个体验与真实电话来电完全一致。

---

## 2. 目标用户

- 需要从尴尬社交场合脱身的用户
- 需要打断无聊会议或约会的用户
- 需要一个"紧急借口"的任意用户

---

## 3. 核心功能需求

### 3.1 主界面

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **随机来电（一键触发）** | **无需任何配置，点击按钮随机生成联系人信息并立即触发来电** | **P0** |
| 立即触发来电 | 点击按钮，1~3 秒后触发模拟来电（使用自定义联系人） | P0 |
| 定时触发来电 | 设置倒计时（如 1 分钟后），到时自动触发 | P0 |
| 来电者信息配置 | 设置显示的来电姓名、号码、头像 | P0 |
| 快速预设 | 保存多个常用联系人预设，一键切换 | P1 |
| 延迟时间选择 | 快速选择 30s / 1min / 2min / 5min 或自定义 | P0 |

#### 随机来电功能说明

- 随机从内置名字库中抽取丹麦姓名或关系称呼（Mor / Far / Chef / Tandlæge 等）
- 随机生成符合丹麦规范的手机号（8位，格式：`XX XX XX XX`）
- 随机选取内置默认头像之一
- 生成的联系人信息**不保存**，用完即丢
- 触发后流程与普通来电完全一致

### 3.2 来电界面（Incoming Call Screen）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 全屏来电通知 | 与系统原生来电界面视觉一致 | P0 |
| 显示来电人信息 | 头像、姓名、号码 | P0 |
| 接听按钮 | 滑动或点击接听 | P0 |
| 拒接按钮 | 滑动或点击拒接，直接结束 | P0 |
| 来电铃声 | 播放系统默认铃声或自定义铃声 | P0 |
| 振动 | 随铃声振动 | P0 |
| 锁屏触发 | 在锁屏状态下弹出来电界面 | P0 |

### 3.3 通话中界面（In-Call Screen）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 通话计时器 | 显示通话时长，从 00:00 开始计时 | P0 |
| 对方信息展示 | 头像、姓名 | P0 |
| 挂断按钮 | 点击结束通话 | P0 |
| 静音按钮 | 静音麦克风（UI 状态切换） | P1 |
| 扬声器按钮 | 切换扬声器（UI 状态切换） | P1 |
| 模拟通话音频（可选） | 播放预录的"对方声音"音频片段 | P2 |
| 数字键盘按钮 | 展示拨号键盘（UI） | P1 |

### 3.4 通话结束界面

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 通话结束提示 | 显示"通话已结束"及通话时长 | P0 |
| 自动返回主界面 | 2 秒后自动跳回主界面 | P0 |

### 3.5 联系人界面（My Contacts）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 联系人列表 | 显示所有已保存的来电者预设，支持删除 | P0 |
| 设为默认 | 标记某个联系人为当前默认来电者 | P0 |
| 新增联系人 | 输入姓名、号码，选填头像 | P0 |
| 来电者姓名 | 文本输入，**必填** | P0 |
| 来电者号码 | 文本输入，**必填** | P0 |
| 来电者头像 | 从相册选取，**可选**，不填则使用默认头像 | P1 |

### 3.6 锁屏来电

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 锁屏弹出来电界面 | 使用 Full-Screen Intent 穿透锁屏 | P0 |
| 亮屏唤醒 | 来电时自动点亮屏幕 | P0 |

---

## 4. 非功能需求

- **兼容性**: Android 8.0 (API 26) 及以上
- **权限**: 不需要真实拨号权限，不发起真实通话
- **隐私**: 不联网，所有数据本地存储
- **性能**: 来电触发延迟 ≤ 200ms（从定时到来电弹出）
- **UI 风格**: Material Design 3，深色主题优先（参考设计稿）

---

## 5. 权限清单

| 权限 | 用途 |
|------|------|
| `VIBRATE` | 来电振动 |
| `USE_FULL_SCREEN_INTENT` | 锁屏来电界面 |
| `WAKE_LOCK` | 亮屏唤醒 |
| `RECEIVE_BOOT_COMPLETED` | 重启后恢复定时任务 |
| `POST_NOTIFICATIONS` | Android 13+ 通知权限 |

---

## 6. 用户流程

```
主界面
  ├── 随机来电（一键）→ [随机生成联系人] → 来电界面
  ├── 自定义来电 → 选择或使用默认联系人 → 触发来电
  │     ├── 立即触发 → [等待 ~1.5s] → 来电界面
  │     └── 定时触发 → [倒计时] → 来电界面
  │
联系人界面（My Contacts）
  ├── 查看 / 删除已保存联系人
  └── 新增联系人 → 填写信息 → 保存
  │
来电界面
  ├── 接听 → 通话中界面 → [挂断] → 结束界面 → 主界面
  └── 拒接 → 主屏幕
```

---

## 7. 设计参考

设计稿位于 `/Users/libowen/Desktop/fakecall.pen`，包含以下 5 个屏幕：
1. 主界面 / 联系人选择
2. 来电界面
3. 通话中界面
4. 通话结束界面
5. 锁屏来电界面
